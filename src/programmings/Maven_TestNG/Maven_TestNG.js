// Maven_TestNG.js
import React from 'react';

import useDocumentTitle from '../../hooks/useDocumentTitle';

const Maven_TestNG = () => {
    const [document_title, setDocumentTitle] = useDocumentTitle("ROR:Maven & TestNG");
    return (<h1> Maven & TestNG:</h1>);
}

export default Maven_TestNG;
